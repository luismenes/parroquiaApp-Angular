// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyA_0D3zPbXqccb4sZB28lX_sLji9obAZ-0",
    authDomain: "loginparromomil.firebaseapp.com",
    databaseURL: "https://loginparromomil.firebaseio.com",
    projectId: "loginparromomil",
    storageBucket: "loginparromomil.appspot.com",
    messagingSenderId: "120789740092",
    appId: "1:120789740092:web:af7c4d3a7ccd25bf3ff139",
    measurementId: "G-8PTFJ12XH2"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
